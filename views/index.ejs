<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WebGIS Du lịch</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.3/dist/leaflet.css" crossorigin="anonymous" />
</head>
<body>
  <%- include('partials/header') %>

  <div class="container">
    <h1>WebGIS Du lịch</h1>

    <% if (typeof error !== 'undefined' && error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <% if (user) { %>
      <div id="map" style="height: calc(100vh - 180px); width: 100%; border-radius: 12px; margin-top: 16px;"></div>
    <% } else { %>
      <div class="card" style="text-align: center; padding: 32px; margin-top: 16px;">
        <p style="color: var(--muted); margin-bottom: 16px;">Bạn cần đăng nhập để xem bản đồ và tạo lịch trình.</p>
        <div style="display: flex; gap: 12px; justify-content: center;">
          <a href="/user/login" class="btn btn-primary">Đăng nhập</a>
          <a href="/user/register" class="btn btn-ghost">Đăng ký</a>
        </div>
      </div>
    <% } %>
  </div>

  <script src="https://unpkg.com/leaflet@1.9.3/dist/leaflet.js" crossorigin="anonymous"></script>
  <script src="/js/main.js" defer></script>
</body>
</html>